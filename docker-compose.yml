services:
  companeges:  
    image: bitnami/postgresql:latest
    ports: 
      - '5432:5432'
    environment:
      - POSTGRES_USER=juvino
      - POSTGRES_PASSWORD=juvino123
      - POSTGRES_DB=companeges
    volumes:
      - companeges_pg_data:/bitnami/postgresql

volumes:
  companeges_pg_data:
