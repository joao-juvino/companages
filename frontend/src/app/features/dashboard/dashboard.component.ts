import { Component,OnInit,signal } from '@angular/core'; import { DatePipe } from '@angular/common'; import { RouterLink } from '@angular/router'; import { ApiService } from '../../core/api.service'; import { DashboardSummary } from '../../core/models';
@Component({imports:[DatePipe,RouterLink],template:`<div class="page-head"><div><span class="eyebrow">OVERVIEW</span><h1>Dashboard</h1><p>Everything that matters across your workspaces.</p></div><a class="btn primary" routerLink="/organizations">Manage organizations</a></div>@if(loading()){<div class="state"><span class="spinner"></span>Loading your workspace…</div>}@else if(error()){<div class="state error-box">{{error()}} <button class="text-btn" (click)="load()">Try again</button></div>}@else if(data();as d){<div class="metrics"><article><span class="metric-icon violet">▦</span><div><small>ORGANIZATIONS</small><strong>{{d.organizations}}</strong></div></article><article><span class="metric-icon blue">♙</span><div><small>MEMBERS</small><strong>{{d.members}}</strong></div></article><article><span class="metric-icon amber">◇</span><div><small>POSITIONS</small><strong>{{d.positions}}</strong></div></article><article><span class="metric-icon green">↗</span><div><small>ASSIGNMENTS</small><strong>{{d.assignments}}</strong></div></article></div><div class="content-grid"><section class="card"><div class="card-head"><div><h2>Recent members</h2><p>Latest people added to your organizations.</p></div></div>@if(d.recentMembers.length){<div class="list">@for(m of d.recentMembers;track m.id){<div class="list-row"><span class="avatar soft">{{m.name[0]}}</span><div class="grow"><strong>{{m.name}}</strong><small>{{m.email||'No email provided'}}</small></div><time>{{m.createdAt|date:'MMM d'}}</time></div>}</div>}@else{<div class="empty"><span>♙</span><h3>No members yet</h3><p>Create an organization and add your first teammate.</p><a class="btn secondary" routerLink="/organizations">Get started</a></div>}</section><aside class="card accent-card"><span class="eyebrow">QUICK START</span><h2>Shape your first team</h2><p>Create an organization, add the people and define clear positions.</p><a routerLink="/organizations">Open organizations →</a></aside></div>}`})
export class DashboardComponent implements OnInit{data=signal<DashboardSummary|null>(null);loading=signal(true);error=signal('');constructor(private api:ApiService){}ngOnInit(){this.load();}load(){this.loading.set(true);this.error.set('');this.api.dashboard().subscribe({next:d=>{this.data.set(d);this.loading.set(false);},error:()=>{this.error.set('We could not load the dashboard.');this.loading.set(false);}});}}
