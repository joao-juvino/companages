import { Component,OnInit,signal } from '@angular/core'; import { FormControl,FormGroup,ReactiveFormsModule,Validators } from '@angular/forms'; import { ActivatedRoute,RouterLink } from '@angular/router'; import { forkJoin } from 'rxjs'; import { ApiService } from '../../core/api.service'; import { Assignment,Member,Organization,Position } from '../../core/models'; import { ToastService } from '../../core/toast.service';
@Component({imports:[ReactiveFormsModule,RouterLink],template:`<a class="back" routerLink="/organizations">← All organizations</a>@if(loading()){<div class="state"><span class="spinner"></span>Opening workspace…</div>}@else if(error()){<div class="state error-box">{{error()}}</div>}@else if(org();as o){<div class="detail-hero"><div class="org-mark big">{{o.name[0]}}</div><div><span class="eyebrow">ORGANIZATION</span><h1>{{o.name}}</h1><p>{{o.description||'No description yet.'}}</p></div></div><div class="mini-metrics"><div><strong>{{members().length}}</strong><span>Members</span></div><div><strong>{{positions().length}}</strong><span>Positions</span></div><div><strong>{{assignments().length}}</strong><span>Assignments</span></div></div><div class="tabs"><button [class.active]="tab()==='members'" (click)="tab.set('members')">Members</button><button [class.active]="tab()==='positions'" (click)="tab.set('positions')">Positions</button><button [class.active]="tab()==='assignments'" (click)="tab.set('assignments')">Assignments</button></div><section class="card table-card">@if(tab()==='members'){<div class="card-head"><div><h2>Team members</h2><p>People who belong to this organization.</p></div><button class="btn primary" (click)="openMember()">+ Add member</button></div>@if(members().length){<div class="table">@for(m of members();track m.id){<div class="table-row"><span class="avatar soft">{{m.name[0]}}</span><div class="grow"><strong>{{m.name}}</strong><small>{{m.email||'No email provided'}}</small></div><button class="text-btn" (click)="openMember(m)">Edit</button><button class="danger-link" (click)="deleteMember(m)">Delete</button></div>}</div>}@else{<div class="empty"><span>♙</span><h3>No members yet</h3><p>Add the first person to this team.</p></div>}}@if(tab()==='positions'){<div class="card-head"><div><h2>Positions</h2><p>Define the roles that make this team work.</p></div><button class="btn primary" (click)="openPosition()">+ Add position</button></div>@if(positions().length){<div class="role-grid">@for(p of positions();track p.id){<article><span class="metric-icon violet">◇</span><div class="grow"><strong>{{p.name}}</strong><p>{{p.description||'No description'}}</p></div><button class="text-btn" (click)="openPosition(p)">Edit</button><button class="danger-link" (click)="deletePosition(p)">Delete</button></article>}</div>}@else{<div class="empty"><span>◇</span><h3>No positions yet</h3><p>Add roles such as Backend Developer or Product Designer.</p></div>}}@if(tab()==='assignments'){<div class="card-head"><div><h2>Assignments</h2><p>Connect people with their responsibilities.</p></div><button class="btn primary" [disabled]="!members().length||!positions().length" (click)="assignmentDialog.set(true)">+ Assign position</button></div>@if(assignments().length){<div class="table">@for(a of assignments();track a.id){<div class="table-row"><span class="avatar soft">{{a.member.name[0]}}</span><div class="grow"><strong>{{a.member.name}}</strong><small>{{a.position.name}}</small></div><span class="pill">Active</span><button class="danger-link" (click)="deleteAssignment(a)">Remove</button></div>}</div>}@else{<div class="empty"><span>↗</span><h3>No assignments yet</h3><p>Add members and positions, then connect them here.</p></div>}}</section>}
@if(memberDialog()){<div class="modal-backdrop"><section class="modal"><div class="card-head"><h2>{{editingMember()?'Edit member':'Add member'}}</h2><button class="icon-btn" (click)="memberDialog.set(false)">×</button></div><form [formGroup]="memberForm" (ngSubmit)="saveMember()"><label>Name<input formControlName="name" placeholder="Alex Morgan"></label><label>Email <span class="optional">optional</span><input type="email" formControlName="email" placeholder="alex@company.com"></label><div class="actions"><button type="button" class="btn secondary" (click)="memberDialog.set(false)">Cancel</button><button class="btn primary" [disabled]="memberForm.invalid">Save member</button></div></form></section></div>}
@if(positionDialog()){<div class="modal-backdrop"><section class="modal"><div class="card-head"><h2>{{editingPosition()?'Edit position':'Add position'}}</h2><button class="icon-btn" (click)="positionDialog.set(false)">×</button></div><form [formGroup]="positionForm" (ngSubmit)="savePosition()"><label>Name<input formControlName="name" placeholder="Backend Developer"></label><label>Description <span class="optional">optional</span><textarea rows="3" formControlName="description"></textarea></label><div class="actions"><button type="button" class="btn secondary" (click)="positionDialog.set(false)">Cancel</button><button class="btn primary" [disabled]="positionForm.invalid">Save position</button></div></form></section></div>}
@if(assignmentDialog()){<div class="modal-backdrop"><section class="modal"><div class="card-head"><h2>Assign a position</h2><button class="icon-btn" (click)="assignmentDialog.set(false)">×</button></div><form [formGroup]="assignmentForm" (ngSubmit)="saveAssignment()"><label>Member<select formControlName="memberId"><option [ngValue]="null">Choose a member</option>@for(m of members();track m.id){<option [ngValue]="m.id">{{m.name}}</option>}</select></label><label>Position<select formControlName="positionId"><option [ngValue]="null">Choose a position</option>@for(p of positions();track p.id){<option [ngValue]="p.id">{{p.name}}</option>}</select></label><div class="actions"><button type="button" class="btn secondary" (click)="assignmentDialog.set(false)">Cancel</button><button class="btn primary" [disabled]="assignmentForm.invalid">Create assignment</button></div></form></section></div>}`})
export class OrganizationDetailComponent implements OnInit{id=0;org=signal<Organization|null>(null);members=signal<Member[]>([]);positions=signal<Position[]>([]);assignments=signal<Assignment[]>([]);loading=signal(true);error=signal('');tab=signal<'members'|'positions'|'assignments'>('members');memberDialog=signal(false);positionDialog=signal(false);assignmentDialog=signal(false);editingMember=signal<Member|null>(null);editingPosition=signal<Position|null>(null);memberForm=new FormGroup({name:new FormControl('',{nonNullable:true,validators:[Validators.required]}),email:new FormControl('',{nonNullable:true,validators:[Validators.email]})});positionForm=new FormGroup({name:new FormControl('',{nonNullable:true,validators:[Validators.required]}),description:new FormControl('',{nonNullable:true})});assignmentForm=new FormGroup({memberId:new FormControl<number|null>(null,Validators.required),positionId:new FormControl<number|null>(null,Validators.required)});constructor(private route:ActivatedRoute,private api:ApiService,private toast:ToastService){}ngOnInit(){this.id=Number(this.route.snapshot.paramMap.get('id'));forkJoin({org:this.api.organization(this.id),members:this.api.members(this.id),positions:this.api.positions(this.id),assignments:this.api.assignments(this.id)}).subscribe({next:r=>{this.org.set(r.org);this.members.set(r.members);this.positions.set(r.positions);this.assignments.set(r.assignments);this.loading.set(false);},error:()=>{this.error.set('This organization could not be found or loaded.');this.loading.set(false);}});}openMember(m:Member|null=null){this.editingMember.set(m);this.memberForm.setValue({name:m?.name||'',email:m?.email||''});this.memberDialog.set(true);}saveMember(){if(this.memberForm.invalid)return;const m=this.editingMember(),call=m?this.api.updateMember(this.id,m.id,this.memberForm.getRawValue()):this.api.createMember(this.id,this.memberForm.getRawValue());call.subscribe(v=>{this.members.update(xs=>m?xs.map(x=>x.id===v.id?v:x):[...xs,v]);this.memberDialog.set(false);this.toast.show(m?'Member updated':'Member added');});}deleteMember(m:Member){if(!confirm(`Delete ${m.name}?`))return;this.api.deleteMember(this.id,m.id).subscribe(()=>{this.members.update(v=>v.filter(x=>x.id!==m.id));this.assignments.update(v=>v.filter(x=>x.member.id!==m.id));this.toast.show('Member deleted');});}openPosition(p:Position|null=null){this.editingPosition.set(p);this.positionForm.setValue({name:p?.name||'',description:p?.description||''});this.positionDialog.set(true);}savePosition(){if(this.positionForm.invalid)return;const p=this.editingPosition(),call=p?this.api.updatePosition(this.id,p.id,this.positionForm.getRawValue()):this.api.createPosition(this.id,this.positionForm.getRawValue());call.subscribe(v=>{this.positions.update(xs=>p?xs.map(x=>x.id===v.id?v:x):[...xs,v]);this.positionDialog.set(false);this.toast.show(p?'Position updated':'Position added');});}deletePosition(p:Position){if(!confirm(`Delete ${p.name}?`))return;this.api.deletePosition(this.id,p.id).subscribe(()=>{this.positions.update(v=>v.filter(x=>x.id!==p.id));this.assignments.update(v=>v.filter(x=>x.position.id!==p.id));this.toast.show('Position deleted');});}saveAssignment(){const v=this.assignmentForm.getRawValue();if(v.memberId==null||v.positionId==null)return;this.api.createAssignment(this.id,{memberId:v.memberId,positionId:v.positionId}).subscribe({next:a=>{this.assignments.update(x=>[a,...x]);this.assignmentDialog.set(false);this.assignmentForm.reset();this.toast.show('Assignment created');},error:e=>this.toast.show(e.error?.message||'Unable to create assignment')});}deleteAssignment(a:Assignment){this.api.deleteAssignment(this.id,a.id).subscribe(()=>{this.assignments.update(v=>v.filter(x=>x.id!==a.id));this.toast.show('Assignment removed');});}}
