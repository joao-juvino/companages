import { TestBed } from '@angular/core/testing'; import { provideHttpClient } from '@angular/common/http'; import { provideHttpClientTesting,HttpTestingController } from '@angular/common/http/testing'; import { AuthService } from './auth.service';
describe('AuthService',()=>{let service:AuthService;let http:HttpTestingController;beforeEach(()=>{localStorage.clear();TestBed.configureTestingModule({providers:[provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(AuthService);http=TestBed.inject(HttpTestingController);});afterEach(()=>http.verify());it('stores token and updates authentication after login',()=>{expect(service.authenticated()).toBe(false);service.login({email:'ana@example.com',password:'password'}).subscribe();const req=http.expectOne('/api/auth/login');req.flush({token:'jwt-token',user:{id:1,name:'Ana',email:'ana@example.com',createdAt:''}});expect(service.token).toBe('jwt-token');expect(service.authenticated()).toBe(true);expect(service.user()?.name).toBe('Ana');});it('clears the session on logout',()=>{service.login({email:'ana@example.com',password:'password'}).subscribe();http.expectOne('/api/auth/login').flush({token:'token',user:{id:1,name:'Ana',email:'ana@example.com',createdAt:''}});service.logout();expect(service.token).toBeNull();expect(service.authenticated()).toBe(false);});});
