import { HttpClient } from '@angular/common/http'; import { Injectable } from '@angular/core'; import { Assignment,DashboardSummary,Member,Organization,Position } from './models';
@Injectable({providedIn:'root'}) export class ApiService{constructor(private http:HttpClient){}dashboard(){return this.http.get<DashboardSummary>('/api/dashboard');}organizations(){return this.http.get<Organization[]>('/api/organizations');}organization(id:number){return this.http.get<Organization>(`/api/organizations/${id}`);}createOrganization(v:{name:string;description:string}){return this.http.post<Organization>('/api/organizations',v);}updateOrganization(id:number,v:{name:string;description:string}){return this.http.put<Organization>(`/api/organizations/${id}`,v);}deleteOrganization(id:number){return this.http.delete<void>(`/api/organizations/${id}`);}members(id:number){return this.http.get<Member[]>(`/api/organizations/${id}/members`);}createMember(id:number,v:{name:string;email:string}){return this.http.post<Member>(`/api/organizations/${id}/members`,v);}updateMember(id:number,mid:number,v:{name:string;email:string}){return this.http.put<Member>(`/api/organizations/${id}/members/${mid}`,v);}deleteMember(id:number,mid:number){return this.http.delete<void>(`/api/organizations/${id}/members/${mid}`);}positions(id:number){return this.http.get<Position[]>(`/api/organizations/${id}/positions`);}createPosition(id:number,v:{name:string;description:string}){return this.http.post<Position>(`/api/organizations/${id}/positions`,v);}updatePosition(id:number,pid:number,v:{name:string;description:string}){return this.http.put<Position>(`/api/organizations/${id}/positions/${pid}`,v);}deletePosition(id:number,pid:number){return this.http.delete<void>(`/api/organizations/${id}/positions/${pid}`);}assignments(id:number){return this.http.get<Assignment[]>(`/api/organizations/${id}/assignments`);}createAssignment(id:number,v:{memberId:number;positionId:number}){return this.http.post<Assignment>(`/api/organizations/${id}/assignments`,v);}deleteAssignment(id:number,aid:number){return this.http.delete<void>(`/api/organizations/${id}/assignments/${aid}`);}}
